import DefaultLinkFooter from "Components/link_footer";

const LinkFooter = ({ text, url }) => {
  return <DefaultLinkFooter text={text} url={url} logoPath="logo-white.svg" />;
};

export default LinkFooter;
