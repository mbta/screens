import { ComponentType } from "react";

import { FreeTextType } from "Components/v2/free_text";

type NoticeSection = {
  text: FreeTextType;
};

const NoticeSection: ComponentType<NoticeSection> = () => {
  return null;
};

export default NoticeSection;
